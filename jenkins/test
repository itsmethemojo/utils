#!groovy

node {
    stage 'Checkout'
    checkout scm
    sh "git checkout jenkinsfile"
    stage 'Print Software Versions'
    sh "php --version | head -n1; composer --version"
    stage 'Install Dependencies'
    sh "composer install"
}